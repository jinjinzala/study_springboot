{{> header}}

<h2>게시글 상세</h2>
<div class="mb-3">
    <span>작성자: {{post.writer}}</span>
    <span class="ml-3">작성일: {{post.createdAt}}</span>
</div>
<div class="mb-3">
    <h3>{{post.title}}</h3>
    <div>{{post.content}}</div>
</div>
<div class="text-right">
    <a href="/board/{{post.id}}/edit" class="btn btn-primary">수정</a>
    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">삭제</button>
    <a href="/board" class="btn btn-secondary">목록</a>
</div>

<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">게시글 삭제</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                정말로 게시글을 삭제하시겠습니까?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">취소</button>
                <a href="/board/{{post.id}}/delete" class="btn btn-danger">삭제</a>
            </div>
        </div>
    </div>
</div>

{{> footer}}